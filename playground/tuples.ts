const drink = {
  color: 'brown',
  carbonated: false,
  sugar: 40,
};

type Drink = [string, boolean, number];

const pepsi: Drink = ['black', true, 55];
const sprite: Drink = ['white', true, 47];

const carSpecs: [number, number] = [40, 4325];
const carStats = {
  horsePower: 40,
  weight: 4325,
};
